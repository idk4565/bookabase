package actions

import CommandCallback

object CollectionActions {
    val listCollections: CommandCallback = { state, _ -> }

    val enterCollection: CommandCallback = { state, _ -> }

    val updateCollection: CommandCallback = { state, _ -> }

    val addToCollection: CommandCallback = { state, _ -> }

    val removeFromCollection: CommandCallback = { state, _ -> }

    val deleteCollection: CommandCallback = { state, _ -> }

    val exitCollection: CommandCallback = { state, _ -> }
}